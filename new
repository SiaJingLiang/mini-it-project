def addBooks():
    categoryList =['literature', 'encyclopedia', 'guidlines', 'motivations', 'dictionary', 'history', 'news', 'others']
    conn = sqlite3.connect('books.db')
    c = conn.cursor()
    c.execute('''CREATE TABLE IF NOT EXISTS BOOKS 
                    (ID INT NOT NULL, 
                    TITLE TEXT PRIMARY NOT NULL,
                    AUTHOR TEXT,
                    CATEGORY TEXT NOT NULL, 
                    AMOUNT INT NOT NULL,
                    AMOUNTLEFT INT,
                    PRICE REAL NOT NULL,
                    DATEADDED TIMESTAMP NOT NULL, 
                    BORROWEDBY TEXT,
                    BORROWEDDATE TIMESTAMP)''')
    title = input("Title: ")
    fetch  = c.execute('SELECT TITLE from BOOKS WHERE TITLE=?', (title,))
    if fetch.fetchnone != None:
        choice = str(input("Book exists, do you want to increase amount only? y/n "))
        if choice == "y":
            find = c.execute('SELECT * from BOOKS WHERE TITLE=?', (title))
            for row in find:
                amount = row[3]
                print("Amount is", amount)
                return amount
            newAmount = input("Enter amount to be added. ")
            amount += newAmount
            c.execute("UPDATE COMPANY set AMOUNT = ? WHERE TITLE = ?", (amount, title))

    catChoice = input("[1]Literature\n[2]Encyclopedia\n[3]Guidlines\n[4]Motivations\n[5]Dictionary\n[6]History\n[7]News\n[8]Others\n[9]Back\nChoice: ")
    while catChoice < 1 or catChoice >9:
        print("Input invalid. ")
        catChoice = input("[1]Literature\n[2]Encyclopedia\n[3]Guidlines\n[4]Motivations\n[5]Dictionary\n[6]History\n[7]News\n[8]Others\n[9]Back\nChoice: ")
    if catChoice == 9:
        menu()
        quit()
    else:
        category = categoryList[catChoice-1]
    
    amount = int(input("Enter amount: "))
    while amount == '' or amount <= 0 or amount.isdigit() == False:
        print("Invalid")
        amount = int(input("Enter amount: "))
    id = str(langChoice + catChoice + ficChoice)
